# Use PDM to generate requirements.txt
```shell
pdm list --freeze > requirements.txt
```

# Dockerfiles
```shell
1. Refer to ./api/Dockerfile
2. Refer to ./frontend/Dockerfile
```

# Docker compose
```shell
docker-compose build
```